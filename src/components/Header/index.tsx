import React from 'react';

 import { Container } from './styles';

const Header: React.FC = () => {
  return (
    <Container>
      <h2>Drag & Drop Example with React</h2>
    </Container>
  );
}

export default Header;
